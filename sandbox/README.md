# CodeForge Sandbox

This directory will contain the environment scripts and Dockerfiles for compiling, testing, and verifying AI-generated code changes safely in an isolated environment.

## Sandbox Requirements
- CPU and Memory constraints
- No internet access (to prevent data exfiltration)
- Local mount of the checkout repository for fast compilation
