# CodeForge AI Integration Tests

This folder will house cross-system tests and integration validation scripts that involve both frontend mock requests, backend databases, and sandbox task execution scripts.

## Current Setup
- Backend unit and integration tests are situated inside `backend/tests/` using `pytest`.
- Frontend component tests are situated inside `frontend/` using Vitest / React Testing Library.
