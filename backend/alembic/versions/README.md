# Alembic Migrations Versions

This folder contains auto-generated database migration scripts representing schema changes over time.
Migrations are generated using:
`alembic revision --autogenerate -m "description"`
And applied using:
`alembic upgrade head`
